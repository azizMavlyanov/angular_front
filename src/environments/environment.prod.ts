export const environment = {
  production: true,
  api: {
    base: "http://localhost:8000",
    prefix: "/api/",
    authPrefix: "auth/",
    login: "login/",
    registration: "register/",
    entries: "articles",
    userEntries: "users",
    photoEntries: "photos"
  }
};
